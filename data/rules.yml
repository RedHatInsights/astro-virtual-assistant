version: "3.1"

rules:
  - rule: User is asked for feedback
    condition:
      - active_loop: feedback_form
    steps:
      - action: feedback_form
      - active_loop: null
      - slot_was_set:
          - requested_slot: null
