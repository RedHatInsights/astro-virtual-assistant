version: "3.1"

rules: 
  - rule: User is asked for feedback
    condition:
      - active_loop: form_feedback
    steps:
      - action: form_feedback
      - active_loop: null
      - slot_was_set:
          - requested_slot: null

